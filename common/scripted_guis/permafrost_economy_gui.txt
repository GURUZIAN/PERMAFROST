scripted_gui = {

	food_rations = {
		context_type = player_context
		parent_window_token = top_bar
		window_name = "food_rations_container"

		visible = {
		}

        ai_enabled = {
		    always = no
        }
	}
	energy = {
		context_type = player_context
		parent_window_token = top_bar
		window_name = "energy_container"

		visible = {
		}

        ai_enabled = {
		    always = no
        }
	}  
}


## New Station Economy Mechanics ##


scripted_gui = {
	context_type = selected_state_context

	window_name = "statePF_economy_window"
	parent_window_token = selected_state_view
	visible = {
		ROOT = {
			is_ai = no
		}
		OR = {
			is_in_array = { global.statePF = THIS.id }
			any_of_scopes = {
				array = global.statePF
				is_in_array = { controlled_stations = PREV.id }
			}
		}
	}
}
