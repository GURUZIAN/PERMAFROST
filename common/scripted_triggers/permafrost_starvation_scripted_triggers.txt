
@POP = 5

calculate_statesPF_income = {
	set_temp_variable = { state_population = 0 }

	all_of_scopes = {
		if = {
			limit = {
				is_in_array = {
					array = global.statePF
				}
				multiply_temp_variable = { temp_population = @POP }
			}
		}
	}

	multiply_temp_variable = {
		temp_population = @POP
	}
}
